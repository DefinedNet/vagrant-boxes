d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian12
d-i netcfg/get_domain string localdomain

d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

d-i passwd/root-password password vagrant
d-i passwd/root-password-again password vagrant
d-i passwd/user-fullname string vagrant
d-i passwd/username string vagrant
d-i passwd/user-password password vagrant
d-i passwd/user-password-again password vagrant

d-i clock-setup/utc boolean true
d-i time/zone string UTC
d-i clock-setup/ntp boolean true

d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

tasksel tasksel/first multiselect standard
d-i pkgsel/include string openssh-server sudo extlinux syslinux-common
d-i pkgsel/upgrade select full-upgrade

d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string /dev/sda

d-i preseed/late_command string \
  in-target extlinux --install /boot; \
  ROOTUUID=$(blkid -s UUID -o value /dev/sda1); \
  KERNEL=$(basename $(ls /target/boot/vmlinuz-* | sort -V | tail -1)); \
  INITRD=$(basename $(ls /target/boot/initrd.img-* | sort -V | tail -1)); \
  printf "DEFAULT debian\nLABEL debian\n  KERNEL /boot/%s\n  APPEND root=UUID=%s ro\n  INITRD /boot/%s\n" "$KERNEL" "$ROOTUUID" "$INITRD" > /target/boot/extlinux.conf; \
  dd if=/target/usr/lib/syslinux/mbr/mbr.bin of=/dev/sda bs=440 count=1; \
  sed -i 's/^#PermitRootLogin.*/PermitRootLogin yes/' /target/etc/ssh/sshd_config

d-i finish-install/reboot_in_progress note
