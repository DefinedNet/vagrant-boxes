name: Build Debian 11 i386 Vagrant Box

on:
  workflow_dispatch:
  push:
    paths:
      - 'debian11-i386/**'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: debian11-i386

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd #v6.0.2

      - name: Install dependencies
        run: |
          curl -fsSL https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
          sudo apt-get update && sudo apt-get install -y qemu-system-x86 qemu-utils vagrant packer hcp
          sudo chmod 666 /dev/kvm

      - name: Cache ISO
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae #v5.0.5
        with:
          path: ~/.cache/packer/debian-11-i386.iso
          key: debian-11.11.0-i386-netinst-iso
          save-always: true

      - name: Download ISO
        run: |
          mkdir -p ~/.cache/packer
          if [ ! -f ~/.cache/packer/debian-11-i386.iso ]; then
            curl -fSL -o ~/.cache/packer/debian-11-i386.iso https://cdimage.debian.org/cdimage/archive/11.11.0/i386/iso-cd/debian-11.11.0-i386-netinst.iso
          fi
          echo "cc7bb888562a02e9f49bc93233c49be1f2decdb9934bd5b805f114a4eeb9e052  $HOME/.cache/packer/debian-11-i386.iso" | sha256sum -c

      - name: Init Packer plugins
        run: packer init debian11-i386.pkr.hcl

      - name: Build box
        run: PACKER_LOG=1 packer build -on-error=abort -var "iso_url=$HOME/.cache/packer/debian-11-i386.iso" debian11-i386.pkr.hcl

      - name: Upload box as artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a #v7.0.1
        with:
          name: debian11-i386-libvirt
          path: debian11-i386/debian11-i386-libvirt.box

      - name: Upload serial log on failure
        if: failure() || cancelled()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a #v7.0.1
        with:
          name: serial-log
          path: debian11-i386/serial.log

      - name: Upload qcow2 on failure (for local inspection)
        if: failure() || cancelled()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a #v7.0.1
        with:
          name: failed-qcow2
          path: debian11-i386/output-debian11-i386/packer-debian11-i386

      - name: Publish to HCP Vagrant Registry
        if: github.ref == 'refs/heads/main'
        env:
          HCP_CLIENT_ID: ${{ secrets.HCP_CLIENT_ID }}
          HCP_CLIENT_SECRET: ${{ secrets.HCP_CLIENT_SECRET }}
        run: |
          hcp auth login --client-id="$HCP_CLIENT_ID" --client-secret="$HCP_CLIENT_SECRET"
          vagrant cloud publish \
            --force \
            --release \
            DefinedNet/debian11-i386 \
            1.0.${{ github.run_number }} \
            libvirt \
            debian11-i386-libvirt.box
